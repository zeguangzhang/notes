## 问答数据mongo输出导出json文件本机位置：
```
/Users/我得用户名/data    
event0 -event2.json分别是从mongo本地和mongo open-stack机器拷导下来的数据，数据不重叠，只不过数据太多，爬到指定量级手动分库了、
``

## opem-stack:除了自己用户目录数据之外，其他占用空间的数据房子啊了/data/zhangzeguang/下面


## open项目：
需求改动：
--set字段没有不展示 还是值显示空问题 代码写了又删 为了处理那种需求代码不是很好写
-- tag问题调整 以前的switch方法就没用了，又改回去了，
-- 其他规则的变化，这些甚至可以让归一化去做，现在open还要处理这些规则，
-- 每次改完不是我改完就行了，每次我还得做开发测试，这都比较浪费时间

总体需求：可以改动，但是不要频繁改动，但是整体项目要有个周期 OPEN平台本质就是对外输出内容，而不是做各种中间逻辑处理